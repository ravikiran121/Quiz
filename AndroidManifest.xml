<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.QDRoid.Database"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk android:minSdkVersion="7" />

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name" >
        <activity
            android:name=".Splash"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:label="@string/app_name" android:name=".Menu"></activity>
         <activity android:label="@string/app_name" android:name=".AndroidActivity"></activity>
        <activity android:label="@string/app_name" android:name=".QuizActivity"></activity>
        <activity android:label="@string/app_name" android:name=".InsertUserAnswer"> </activity>
    </application>

</manifest>